# VpsPack
####### apt-get update && apt-get upgrade && apt-get install ca-certificates && wget https://raw.githubusercontent.com/manahkl/VpsPack/master/install && bash install && bash conf && VpsPack

